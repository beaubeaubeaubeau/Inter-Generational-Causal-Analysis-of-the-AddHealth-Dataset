# Data (restricted AddHealth files — never commit)
data/

# outputs/: keep only the markdown reports; ignore CSVs, PNGs, and the parquet cache
outputs/*
!outputs/*.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# Editors / IDE
.vscode/
.idea/
*.swp
*.swo

# macOS
.DS_Store

# Claude Code local settings (machine-specific)
.claude/settings.local.json
