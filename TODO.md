# TODO — Inter-Generational Causal Analysis of Add Health

Exhaustive backlog as of 2026-04-22. Organised by kind, prioritised within each section (P0 = blocker / correctness; P1 = important / near-term; P2 = polish / quality-of-life).

Conventions:
- **Ref** — where the issue was surfaced or where the fix belongs.
- Strikethrough marks items completed in this session and left for traceability.

---

## A. Research pipeline — Task 16 and beyond

The following are the analytic next steps called out in [reference/research_journal.md](reference/research_journal.md#outstanding-uncertainties) and [reference/methods.md §1](reference/methods.md#1-identification-assumptions-and-target-estimand). Each item that has an associated experiment is registered in [experiments/README.md](experiments/README.md) with an `EXP-16-*` ID; cross-references below. Ordered roughly by sequencing — later items assume earlier items are done.

- **[P1, downgraded from P0] Front-door / IV decomposition for AHPVT — now a SENSITIVITY check.** Per the 2026-04-25 trajectory-framing decision ([methods.md §1](reference/methods.md#1-identification-assumptions-and-target-estimand)), AHPVT serves as the W1 baseline cognitive measure; the front-door is no longer load-bearing for primary identification. **Purpose now:** quantify how much the EXP-14-COG trajectory β shifts under the strict-mediator reading of AHPVT. Registered as [`EXP-16-FD-COG`](experiments/cognitive-frontdoor/README.md); DAG stub `DAG-Cog-FrontDoor` in [dag_library.md](reference/dag_library.md).
- **[P0] Clean negative-control battery.** Registered as [`EXP-16-NC`](experiments/negative-control-battery/README.md). Pre-flight: verify which candidates (blood type, age at menarche, hand-dominance, residential stability pre-W1) are actually in public-use before drafting the DAG addendum.
- **[P0] IPAW for W5 outcomes.** Implement `scripts/analysis/wls.py:fit_ipaw(design_frame, outcome_frame, covariates)` → stabilised IPAW weights, trim at 95th percentile, layer on `GSW5`. Required before any W5 point estimate is reported.
- **[P1] Per-outcome adjustment sets / DAGs.** Stubs in [dag_library.md](reference/dag_library.md): `DAG-CardioMet`, `DAG-SES`, `DAG-Mental`, `DAG-Functional`. Each needs to be locked in a working session with the user before associated EXP-16-HAND-* experiments execute.
- **[P1] Sibling-dissociation pairs for non-cognitive outcomes.** Extend `SIBLINGS` in [experiments/cognitive-screening/run.py](experiments/cognitive-screening/run.py) (causal-screening block) with per-outcome overrides; re-run [experiments/multi-outcome-screening/run.py](experiments/multi-outcome-screening/run.py) with D3 active. The four EXP-16-HAND-* experiments need this.
- ~~**[P1] Saturated-school stratification / sensitivity.**~~ **Resolved 2026-04-25:** decided to **restrict** the network-exposure estimand to within-saturated-schools (no extrapolation, no saturation-propensity weighting). Registered as [`EXP-16-SAT-BAL`](experiments/saturation-balance/README.md) for the balance table that quantifies external-validity gap.
- **[P1] E-value / VanderWeele sensitivity bounds on the 4 handoff pairs.** Pairs registered as `EXP-16-HAND-IDGX2-WAIST`, `EXP-16-HAND-IDGX2-BMI`, `EXP-16-HAND-IDGX2-BMICLS` in [experiments/cardiometabolic-handoff/README.md](experiments/cardiometabolic-handoff/README.md), and `EXP-16-HAND-ODGX2-EARN` in [experiments/ses-handoff/README.md](experiments/ses-handoff/README.md). Each gets an E-value column for its primary β. Report the minimum unmeasured-confounder strength required to explain the result away.
- **[P1] Ordered-logit / interval-regression for `H5EC1`, `H5LM5`, `H4BMICLS`.** Folded into the per-handoff registrations: `EXP-16-HAND-IDGX2-BMICLS` uses ordered logit, `EXP-16-HAND-ODGX2-EARN` uses interval regression on bracket midpoints (combined with IPAW for W5 attrition).
- **[P1] Bring `H4TO*` medication flags into the L1 covariate set for `H4SBP` / `H4DBP`.** Anti-hypertensive use is not currently adjusted; the null D4 on blood-pressure outcomes could be mechanical. Audit which `H4TO*` items are public-use and test sensitivity.
- **[P2] W6 self-reported memory (`H6DA18B`–`H6DA18F`) as a tertiary cognitive outcome.** Public-use W6 has no performance cognition but does have self-reported memory items; these are subjective but add longitudinal signal. Low priority because construct validity is weaker than the W4 battery. Ref: [dataset_manual.md §4.6](reference/dataset_manual.md).
- **[P2] Manski bounds as a fallback when IPAW is not credible.** When the attrition-probability model has large variance or sparse support in some strata, Manski-style bounds on the outcome effect (best-case / worst-case imputations for attritted cells) are more honest than a single IPAW estimate. Keep in the back pocket for handoff pairs whose IPAW fit is noisy.

---

## B. Correctness / real bugs

- **[P0] `scripts/analysis/derivation.py` — `derive_w5_bds` NaN handling.** `success = (a == 1) | (b == 1)` returns `False` when `a`/`b` contain NaN (NaN == 1 → False), so a respondent with all missing items at length L=4+ but passes at L=2,3 gets a final score of 3 — indistinguishable from a respondent who actually failed at L=4. The end-of-function mask only catches *entirely*-missing respondents. Decide: is "missing at length L" a fail (current behaviour, arguably OK per the protocol since the test terminates after two wrongs) or indeterminate (score = NaN)? If the former, add a comment clarifying; if the latter, add per-length missingness propagation.
- **[P0] D6 threshold drift between code and docs.** ~~Code in the causal-screening block of [experiments/cognitive-screening/run.py](experiments/cognitive-screening/run.py) uses `abs(rho) > 0.7`; methods.md §2 used to say "≥ 0.6". **Fixed the doc to match the code** 2026-04-22 so the ground truth is consistent.~~ Remaining TODO: **decide whether 0.7 is the right threshold** — it's stricter than most dose-response conventions; consider loosening to 0.6 and documenting the change (would reclassify some Weakened exposures).
- ~~**[P1] `scripts/analysis/derivation.py` — misleading comment in `derive_school_belonging`.** Comment says "higher prejudice = stays" immediately before a line that reverses the scale (so that higher score = belonging, not prejudice). Logic is correct but comment is wrong and cost a reviewer cycle. Delete the comment (the reverse-score line speaks for itself) or rewrite it. — Done 2026-04-25.~~
- ~~**[P1] `experiments/cognitive-screening/run.py` (causal-screening block) — hardcoded saturation fraction `0.324`.** `d8_saturation` returns `frac = 0.324 if requires_sat else 0.0`. This should be computed from the actual data (count of network-file-valid AIDs divided by W1 in-home AIDs) rather than a magic number, so the value tracks the dataset if the analytic frame changes. Ref: reviewer A punch-list. — Done 2026-04-25.~~ Replaced with module-level `SATURATED_LOSS_FRAC = 1 - n_network_valid / n_w1inhome` computed at import-time from `cache/w1network.parquet` (IDGX2 non-null = 4,397) and `cache/w1inhome.parquet` (N = 6,504). Current value `0.323954` (matches the prior 0.324 magic number to 3 sig figs). The cognitive screen was re-run; D8 column in `experiments/cognitive-screening/tables/primary/14_screening_matrix.csv` now reads `0.3239544895448955` for all network exposures and `0.0` for survey exposures.
- ~~**[P1] `scripts/prep/outputs/05_weighted_descriptives.md` — PRXPREST row was previously `binary (prop)` (fixed 2026-04-22).** Root cause: `(lo, hi) == (0, 1)` → binary heuristic in [scripts/prep/05_weighted_descriptives.py](scripts/prep/05_weighted_descriptives.py). Fix applied (explicit kinds override); re-run confirmed continuous (mean). Verify no other continuous-in-[0,1] variable hits the same heuristic. Audit other unit-interval continuous centralities (`ERDEN`, `ESDEN`, `ESRDEN`, `RCHDEN` — density variables in [0, 1]) — **check** whether they're routed through the same binary heuristic in any descriptive script. — Done 2026-04-25.~~ Audited `ERDEN`, `ESDEN`, `ESRDEN`, `RCHDEN`: defined with `(0, 1)` in `scripts/analysis/cleaning.py` `VALID_RANGES` but **not** referenced by `scripts/prep/05_weighted_descriptives.py` (its W1-network variable list is `IDGX2/ODGX2/BCENT10X/REACH/REACH3/PRXPREST/HAVEBMF/HAVEBFF/BMFRECIP/BFFRECIP` — densities omitted). The only consumer of the `summarize_block` binary heuristic is `05_weighted_descriptives.py` itself; the verification and causal-screening blocks of `experiments/cognitive-screening/run.py` use `clean_var` only (no kind heuristic). No fix or rerun required.

---

## C. Documentation errors / discrepancies

Cross-checked across [reference/dataset_manual.md](reference/dataset_manual.md), [reference/research_journal.md](reference/research_journal.md), and [reference/variable_dictionary.md](reference/variable_dictionary.md).

- ~~**[P1] Missing from `variable_dictionary.md` quick-ref (but present in detailed / cheat-sheet):** `C5WD90_1`, `H1FS1` (CES-D item 1), `W5_COG_COMP`. Add to quick-ref alphabetical table. — Done 2026-04-25.~~
- ~~**[P1] Missing from `variable_dictionary.md` but cited by `research_journal.md`:** `C4WD60` / `C4WD90` (referenced without the trailing `_1`), `H1GH28` (the W1 self-reported weight covariate that Task 16 should add for `H4BMI`). Either rename the journal references to `C4WD90_1` / `C4WD60_1` or add short-form aliases in the dictionary's quick-ref. — Done 2026-04-25. Renamed the D5 reference in `research_journal.md` to `C4WD90_1` / `C4WD60_1`; added quick-ref + L1-detailed rows for `H1GH28` (label flagged `[label not yet verified]`, TODO retained on the entry).~~
- ~~**[P1] dataset_manual.md deliverables index is missing task09, task12, and task15_journal_figs.** The distribution-plot prep step ([scripts/prep/09_distribution_plots.py](scripts/prep/09_distribution_plots.py)), the cognitive-screening figures module ([experiments/cognitive-screening/figures.py](experiments/cognitive-screening/figures.py)), and the multi-outcome figures module ([experiments/multi-outcome-screening/figures.py](experiments/multi-outcome-screening/figures.py)) produce artefacts but weren't in the index. Add rows with links to `experiments/cognitive-screening/figures/primary/`, `scripts/prep/figures/distributions/`, and the two journal figures. — Done 2026-04-25 (and the deliverables index itself was subsequently subsumed by `experiments/README.md` + `report.md` in the Phase 3 demolition).~~
- **[P2] Variable dictionary lists families but not their full members.** CES-D items `H1FS1`–`H1FS19`, friendship grid `H1MF*`/`H1FF*` items, BEM `H3BM*`, W5 BDS `H5MH*` — only a few exemplar codes are listed. Decide whether to (a) link to the existing per-family files like [scripts/prep/outputs/03a_wave1_friendship_items.md](scripts/prep/outputs/03a_wave1_friendship_items.md), or (b) accept that the dictionary doesn't enumerate family members (current state, lightly documented). Option (a) is a one-sentence pointer per family.
- **[P2] dataset_manual.md §6 "Analytic feasibility" table** still uses "W5 imm" and "W5 backward digit span (derived)" language that conflicts with the §9-era framing of W5 as part of the multi-outcome battery. Mild; the §6 table is about design feasibility, but a one-line note tying §6 row "W1 network ∩ W5 imm" to the expanded outcome battery (now documented in [variable_dictionary.md §2.5](reference/variable_dictionary.md#25-secondary-outcomes--task-15-multi-outcome-extension-12)) would help a new reader see how multi-outcome screening relates to cognitive screening.
- **[P2] N drift between synthesis and journal.** Synthesis §3.1 quotes the raw network-sample N = 4,397; journal Phase 2 quotes the primary analytic frame N = 3,238; dictionary quotes N = 3,268 for IDGX2 in the task14 frame. All three are correct for different stages of the pipeline (raw → mode-OK cognition-merged → covariate-joined), but a one-line note explaining the "4,397 → 3,268 → 3,238" cascade would remove confusion.
- **[P2] Synthesis §1 table says W6 N = 3,937, §2.3 weight table also says 3,937.** Consistent — flagged as no-action, just confirmed.

---

## D. Reproducibility / infrastructure

- ~~**[P0] `requirements.txt` is incomplete.** Missing `seaborn`, `statsmodels`, `pyreadstat` (SAS/XPT reader), `pyarrow` (parquet backend), `scipy` (used by `statsmodels`). Current contents: `ipykernel`, `pandas`, `numpy`, `matplotlib`. Nobody can re-run the pipeline from this as-is. Either pin versions or switch to `pyproject.toml` / `environment.yml`. — Done 2026-04-25.~~
- ~~**[P0] Hardcoded absolute path `/Users/jb/Desktop/Inter-Generational-Causal-Analysis-of-the-AddHealth-Dataset` in 10+ files.** Offenders included `plot_style.py` and the original task01–task13 scripts. Replace with `ROOT = Path(__file__).resolve().parent.parent` (or env var `ADDHEALTH_ROOT`). Non-trivial but mechanical. — Done 2026-04-25.~~
- **[P1] No top-level orchestrator.** No `run_all.py`, `Makefile`, `invoke` / `just` / `snakemake` entry point. A new contributor must read [experiments/README.md](experiments/README.md) to guess the run order. Write a minimal `run_all.py` that invokes the prep pipeline ([scripts/prep/01_file_inventory.py](scripts/prep/01_file_inventory.py) → … → [scripts/prep/09_distribution_plots.py](scripts/prep/09_distribution_plots.py)) followed by [experiments/cognitive-screening/run.py](experiments/cognitive-screening/run.py) and [experiments/multi-outcome-screening/run.py](experiments/multi-outcome-screening/run.py) with `--skip-if-fresh` semantics.
- **[P1] README is effectively empty** ("Test edit" as the second line). Write a real README: project goal, how to get data (public-use ICPSR 21600), install (`pip install -r requirements.txt`), run (`python run_all.py` or explicit sequence), where artefacts land (`scripts/prep/outputs/`, `experiments/<name>/tables/`, `experiments/<name>/figures/`), pointers to the reference docs.
- **[P1] No test suite.** At minimum: unit tests on the derivation helpers (`derive_cesd_sum` on a synthetic frame with known missingness; `derive_w5_bds` with known pass/fail items) and on `clean_var` (reserve-code stripping). Also a smoke test that the cognitive-screening + multi-outcome-screening experiments run end-to-end on the cached parquets and produce non-empty outputs.
- **[P2] Print-based logging throughout.** Scripts use `print()` for progress. No way to suppress or route. Low priority until the repo gets a CI or long-running orchestrator; swap to `logging` module with a configurable level when you do.
- **[P2] SCC deployment path.** User's global instructions note Boston University SCC access with project work under `/projectnb/mcnet/jbrin/`. The pipeline is small enough to run on a laptop, but a sanity check that the scripts tolerate the `$TMPDIR` / `module load miniconda` environment would de-risk moving heavier analyses (Task 16 IPAW re-runs, bootstrap CIs) to the cluster.
- **[P2] `dataset_eda.ipynb` (105 KB) is committed.** Unclear whether it's still authoritative; it predates the tasked pipeline. Either convert the still-relevant parts into task-numbered scripts or mark as archived / move to `docs/legacy/`.
- ~~**[P2] `outputs/cache/` is tracked in `.gitignore` but several parquet files live there.** Confirm `.gitignore`'s `outputs/*` + `!outputs/*.md` is catching `outputs/cache/` correctly (it should be, but git-check: `git check-ignore outputs/cache/w4inhome.parquet`). — Done 2026-04-25. Verified: `git check-ignore -v outputs/cache/w4inhome.parquet` resolves to `.gitignore:5:outputs/*`; no cache files are tracked; no cache `.md` files exist.~~ Note: cache directory has since moved to top-level `cache/` (see Agent G's Phase 4 work); `.gitignore` updated accordingly.

---

## E. Plot & output hygiene

- ~~**[P1] Re-run prep distribution plots + cognitive-screening figures to confirm no baked-in titles remain on any PNG that might get embedded.** This session stripped titles from the 7 PNGs currently embedded in `research_journal.md`; any *future* embedding in the dataset manual or a new brief would need the same treatment. Consider a lint: grep for `![...](path/to.png)` embeddings and verify the source script's corresponding `ax.set_title` is empty. — Done 2026-04-25.~~ Audited all 8 unique embedded PNGs across `reference/*.md` (`screening_heatmap`, `adjustment_stability`, `multi_outcome_beta_heatmap`, `multi_outcome_sig_heatmap`, `15_handoff_forest`, `15_per_outcome_pcount`, `06_attrition_stacked_bar`, `ahpvt_with_without`). Only one had a baked-in title — `multi_outcome_sig_heatmap.png` (in the multi-outcome screening run); stripped and re-ran. The other 7 were already clean.
- ~~**[P2] The `H5LM5` "despite earlier notes" caveat was already removed.** Double-check that no surviving table-output `.md` or chart uses outdated H5LM5 = binary classification. Confirmed clean 2026-04-22; re-verified 2026-04-25 (only surviving `H5LM5.*binary` matches across `*.md` / `*.py` / `*.ipynb` are the `not binary` corrective notes in [variable_dictionary.md](reference/variable_dictionary.md), [dataset_manual.md](reference/dataset_manual.md), [research_journal.md](reference/research_journal.md), and the historical TODO/Changelog entries). — Done 2026-04-25.~~
- ~~**[P2] Consistent colourmap for outcome groups across all charts.** The multi-outcome figures and the journal figures originally used different palettes for outcome group colour. Unified for visual coherence across the report. — Done 2026-04-25.~~ Moved `GROUP_COLORS` into [scripts/analysis/plot_style.py](scripts/analysis/plot_style.py) as the canonical palette; both [experiments/multi-outcome-screening/figures.py](experiments/multi-outcome-screening/figures.py) and [experiments/multi-outcome-screening/run.py](experiments/multi-outcome-screening/run.py) import from it. The multi-outcome heatmap intentionally still uses `RdBu_r` (β z-scores) and `viridis` (−log10 p), since neither encodes outcome group; the import is kept for future per-group charts. Both modules re-run cleanly with no regression.

---

## F. Nice-to-haves / stretch

- **[P2] Plain-text DAG diagram** in [methods.md §1](reference/methods.md#1-identification-assumptions-and-target-estimand). Described textually; rendering as a small `mermaid` block or PNG would be useful for briefings (note: [dag_library.md](reference/dag_library.md) already has the canonical Mermaid `DAG-Cog` block).
- **[P2] Pre-commit hook or CI check** that re-runs the reference-doc cross-consistency audit (quick-ref vs. detailed vs. journal vs. dictionary) and fails on broken anchors / missing variable entries. The audit script in `/tmp` during this session could be crystallised as `scripts/audit_references.py`.
- **[P2] Dictionary quick-ref could support search by role.** Right now it's alphabetical; a reader looking at a forest plot sees `IDGX2 → H5EC1` and wants *all exposures in that forest plot's cluster*. A secondary by-role index at the bottom of the quick-ref would help (or just a link back to the detailed §2.3 subsections).
- ~~**[P2] Decide the repo's canonical citation.** Synthesis references "ICPSR 21600 v26 (2026-03-03)". Lock this into a `CITATION.cff` or a single line in README so downstream work cites consistently. — Done 2026-04-25. Created [CITATION.cff](CITATION.cff) at repo root: ICPSR 21600 v26 dated 2026-03-03, DOI `10.3886/ICPSR21600.v26` flagged `[verify]`, license noted as research-use only with an explicit no-redistribution clause for the underlying microdata.~~

---

## G. Session-completed items (for traceability)

- ~~PRXPREST binary-vs-continuous miscoding fixed in [scripts/prep/05_weighted_descriptives.py](scripts/prep/05_weighted_descriptives.py) + [scripts/prep/outputs/05_weighted_descriptives.md](scripts/prep/outputs/05_weighted_descriptives.md) re-run.~~ **Done 2026-04-22.**
- ~~D3 sibling-dissociation sign-agreement bug.~~ **Fixed earlier in this conversation thread; verified in the causal-screening block of [experiments/cognitive-screening/run.py](experiments/cognitive-screening/run.py).**
- ~~Baked-in titles stripped from the 7 PNGs embedded in `research_journal.md`.~~ **Done 2026-04-22.**
- ~~Variable dictionary restructured into quick-ref + detailed sections.~~ **Done 2026-04-22.**
- ~~Synthesis §5.6 identification-assumptions section, §6.5 D1–D9 methods, Glossary appendix added.~~ **Done 2026-04-22.** (Subsequently moved into [reference/methods.md](reference/methods.md) §1–§2 and [reference/glossary.md](reference/glossary.md) by the Phase 3 demolition.)
- ~~Stata/R recipes removed from synthesis §2.4 (project is Python-only).~~ **Done 2026-04-22.**
- ~~Broken anchor `#45-wave-v-cognitive-battery-...` fixed.~~ **Done 2026-04-22.**
- ~~Stale "earlier notes were wrong" self-references audited and removed or trimmed once source fixes landed.~~ **Done 2026-04-22.**
