# TODO — Inter-Generational Causal Analysis of Add Health

Exhaustive backlog as of 2026-04-26. Organised by kind, prioritised within each section (P0 = blocker / correctness; P1 = important / near-term; P2 = polish / quality-of-life).

Conventions:
- **Ref** — where the issue was surfaced or where the fix belongs.
- Strikethrough marks items completed and left for traceability.
- `[JUDGMENT REQUIRED]` — fix is not mechanical; the user must choose between defensible alternatives. Auto-fix agents skip these.
- Items added 2026-04-26 are from a three-agent audit pass (scientific methodology, code correctness, cross-doc consistency) plus three bugs surfaced while writing test coverage.

---

## A. Research pipeline — Task 16 and beyond

The following are the analytic next steps called out in [reference/research_journal.md](reference/research_journal.md#outstanding-uncertainties) and [reference/methods.md §1](reference/methods.md#1-identification-assumptions-and-target-estimand). Each item that has an associated experiment is registered in [experiments/README.md](experiments/README.md) with an `EXP-16-*` ID; cross-references below. Ordered roughly by sequencing — later items assume earlier items are done.

- **[P1, downgraded from P0] Front-door / IV decomposition for AHPVT — now a SENSITIVITY check.** Per the 2026-04-25 trajectory-framing decision ([methods.md §1](reference/methods.md#1-identification-assumptions-and-target-estimand)), AHPVT serves as the W1 baseline cognitive measure; the front-door is no longer load-bearing for primary identification. **Purpose now:** quantify how much the EXP-14-COG trajectory β shifts under the strict-mediator reading of AHPVT. Registered as [`EXP-16-FD-COG`](experiments/cognitive-frontdoor/README.md); DAG stub `DAG-Cog-FrontDoor` in [dag_library.md](reference/dag_library.md).
- **[P0] Clean negative-control battery.** Registered as [`EXP-16-NC`](experiments/negative-control-battery/README.md). Pre-flight: verify which candidates (blood type, age at menarche, hand-dominance, residential stability pre-W1) are actually in public-use before drafting the DAG addendum.
- **[P0] IPAW for W5 outcomes.** Implement `scripts/analysis/wls.py:fit_ipaw(design_frame, outcome_frame, covariates)` → stabilised IPAW weights, trim at 95th percentile, layer on `GSW5`. Required before any W5 point estimate is reported.
- **[P1] Per-outcome adjustment sets / DAGs.** Stubs in [dag_library.md](reference/dag_library.md): `DAG-CardioMet`, `DAG-SES`, `DAG-Mental`, `DAG-Functional`. Each needs to be locked in a working session with the user before associated EXP-16-HAND-* experiments execute.
- **[P1] Sibling-dissociation pairs for non-cognitive outcomes.** Extend `SIBLINGS` in [experiments/cognitive-screening/run.py](experiments/cognitive-screening/run.py) (causal-screening block) with per-outcome overrides; re-run [experiments/multi-outcome-screening/run.py](experiments/multi-outcome-screening/run.py) with D3 active. The four EXP-16-HAND-* experiments need this.
- ~~**[P1] Saturated-school stratification / sensitivity.**~~ **Resolved 2026-04-25:** decided to **restrict** the network-exposure estimand to within-saturated-schools (no extrapolation, no saturation-propensity weighting). Registered as [`EXP-16-SAT-BAL`](experiments/saturation-balance/README.md) for the balance table that quantifies external-validity gap.
- **[P1] E-value / VanderWeele sensitivity bounds on the 4 handoff pairs.** Pairs registered as `EXP-16-HAND-IDGX2-WAIST`, `EXP-16-HAND-IDGX2-BMI`, `EXP-16-HAND-IDGX2-BMICLS` in [experiments/cardiometabolic-handoff/README.md](experiments/cardiometabolic-handoff/README.md), and `EXP-16-HAND-ODGX2-EARN` in [experiments/ses-handoff/README.md](experiments/ses-handoff/README.md). Each gets an E-value column for its primary β. Report the minimum unmeasured-confounder strength required to explain the result away.
- **[P1] Ordered-logit / interval-regression for `H5EC1`, `H5LM5`, `H4BMICLS`.** Folded into the per-handoff registrations: `EXP-16-HAND-IDGX2-BMICLS` uses ordered logit, `EXP-16-HAND-ODGX2-EARN` uses interval regression on bracket midpoints (combined with IPAW for W5 attrition).
- **[P1] Bring `H4TO*` medication flags into the L1 covariate set for `H4SBP` / `H4DBP`.** Anti-hypertensive use is not currently adjusted; the null D4 on blood-pressure outcomes could be mechanical. Audit which `H4TO*` items are public-use and test sensitivity.
- **[P1] [JUDGMENT REQUIRED] Cluster-robust SE convention — pick one of three.** Ref: scientific audit. The current `weighted_ols` uses `df = H − 1 = 131` from cluster count, but statsmodels' `use_correction=True` already applies its own small-sample factor `H/(H−1) · (n−1)/(n−k)`, mixing two correction philosophies. Three defensible options: (a) trust statsmodels' built-in `use_correction` and use `df = n − k` (statsmodels default); (b) turn `use_correction=False` and apply the H − 1 t-quantile override only; (c) Bell–McCaffrey small-sample correction. Pick one, document, add a unit test against a Stata `_robust` reference output. Source: `scripts/analysis/wls.py:53-61`.
- **[P1] [JUDGMENT REQUIRED] Sibling/twin within-school dependence is unaccounted-for.** Ref: scientific audit. Add Health public-use ships approximate sibling/twin pairs (Wave I oversampled twins, full siblings, half siblings). Cluster-robust SE on `CLUSTER2` only handles between-school clustering, not within-household sibling correlation. Three options: (a) document explicitly that sibling correlation is assumed inside the school PSU and audit how many sibling pairs cross PSUs; (b) implement two-way clustering on `(CLUSTER2, family_id)` if a household-id variable is in public-use; (c) add a sensitivity analysis subsetting to one randomly-chosen respondent per family. Source: `experiments/cognitive-screening/run.py` (every `weighted_ols` call), `reference/methods.md:36`.
- **[P1] Apply BH-FDR within each outcome on the D1 grid (24 tests per outcome).** Ref: scientific audit. The cognitive screen has 24 D1 tests, the multi-outcome screen has 24 × 12 = 288 D1 tests, none with FDR control. While these are screening (ranking-only), the report headline numbers ("12/24 hits on H5EC1", "5 hits on cognition") treat raw p-counts as informative without flagging FDR. Add q-value columns to `14_screening_matrix.csv` and `15_multi_outcome_matrix.csv` per outcome family. Uncontroversial, additive — does not break ranking. Source: scientific audit, `experiments/cognitive-screening/run.py:461`, `reference/methods.md:59`.
- **[P1] Add side-by-side AHPVT-dropped column for SES outcomes in multi-outcome screen.** Ref: scientific audit. The README and `dag_library.md §DAG-SES` correctly state AHPVT should be *dropped* from the SES adjustment set (verbal IQ is on the causal path SOC → AHPVT → educational credentialism → earnings), but the multi-outcome screen still conditions on AHPVT for `H5EC1` and `H5LM5`. Run a parallel screening pass with AHPVT-dropped for SES (and arguably cardiometabolic) outcomes; report both βs side-by-side in the matrix CSV. Source: `experiments/multi-outcome-screening/run.py:139-153`, `experiments/multi-outcome-screening/README.md:17-19`, `reference/dag_library.md:110-114`.
- **[P1] Add saturated-school × covariate-stratum positivity diagnostic to `saturation-balance` experiment.** Ref: scientific audit. The "ATE within saturated schools" estimand is identified only if positivity holds at every level of L0+L1+AHPVT. D7 in the screen only checks one slice. Tabulate joint covariate-stratum cell counts within saturated schools, flag any cell with N < 10 or no exposure variation. Costs nothing additional. Source: `reference/methods.md:34-37`.
- **[P1] AHPVT trajectory framing: empirical sanity check W1 → W3.** Ref: scientific audit. The trajectory framing's load-bearing assumption is that AHPVT is stable-trait g-loaded. Add Health ships a W3 AHPVT (`w3pvt.sas7bdat`) — the cheapest possible test. Regress (W3_PVT − W1_PVT) on W1 social integration with L0+L1 adjustment. If significantly non-zero, the strict mediator reading gains evidence; if not, trajectory framing is corroborated. Add to `cognitive-frontdoor` or as a standalone diagnostic. Source: `reference/methods.md:11-15`, `reference/dataset_manual.md:147-151`.
- **[P1] [JUDGMENT REQUIRED] D3 sibling-dissociation pairs for cognitive screen are dependent constructs, not independent siblings.** Ref: scientific audit. Using `ODGX2` (out-degree) as the sibling for `IDGX2` (in-degree) is questionable — they're two perspectives on the same friendship roster with empirical r ≈ 0.5+. A common confounder pushes both in the same direction. Same problem with `BCENT10X` sibling = `ODGX2`. Re-pair with structurally orthogonal exposures (e.g. `IDGX2` vs `ESDEN`; `BCENT10X` vs `INFLDMN`), or skip D3 for these and rely on D2/D7. Source: `experiments/cognitive-screening/run.py:374-379`.
- **[P1] [JUDGMENT REQUIRED] D6 dose-response is low-power: |ρ| > 0.7 across only 4 free quintile β's is essentially "no inversion".** Ref: scientific audit. Spearman ρ on 4 points is near uninformative. Options: (a) increase to 10 deciles where N permits; (b) replace D6 with formal Wald test of Q5/Q1 contrast against linear-in-quintile-trend; (c) use a generalised additive model partial-residual plot. Document that current D6 is a "no-inversion" check, not a true monotonicity test. Source: `experiments/cognitive-screening/run.py:547-565`, `reference/methods.md:64`.
- **[P1] [JUDGMENT REQUIRED] D4 30% threshold is computed against the post-AHPVT β as denominator — small-β denominator inflates rel-shift.** Ref: scientific audit. `rel_shift = abs(max_shift) / abs(beta_full)`. When AHPVT shrinks β toward 0 (the headline finding), the divisor is tiny and rel-shift blows up — D4 *automatically* fails any exposure where AHPVT eats most of the signal, by mathematical artefact. Add a second D4 metric (absolute shift standardised by L0-β SE or outcome SD) and decide categorisation on the absolute one. User picks which metric drives Strong/Mixed/Weakened. Source: `experiments/cognitive-screening/run.py:496-516`.
- **[P2] Wild-cluster bootstrap on W5 cells in handoff experiments.** Ref: scientific audit. W5 cells hit N as low as ~394 (network-gated) or ~600 (survey-only). With 132 PSUs but most W5-eligible respondents concentrated in fewer school-pairs, effective cluster count for W5 SE is below 132. Add Cameron–Gelbach–Miller wild-cluster bootstrap as sensitivity row in handoff forest; note in multi-outcome README that "p-values for W5 outcomes assume Gaussian CIs that may not hold at N ~ 600 with non-Gaussian outcome scales." Source: `experiments/multi-outcome-screening/run.py` (W5 outcomes), `reference/dataset_manual.md:248-249`.
- **[P2] Expand W5 mode-selection probit covariate set.** Ref: scientific audit. The current probit `P(IS_COG_MODE | IDGX2, male, AHPVT)` underspecifies a survey-design-driven mode assignment. Per `dataset_manual.md §4.5`, mode is mostly Add-Health field choice (Web default, In-person/Telephone for non-respondents to cheaper modes), making it endogenous to SES, geographic accessibility, and prior-wave engagement. Expand probit to include all L0+L1 plus a "prior-wave engagement" proxy. If IDGX2's coefficient remains near zero in richer spec, conclusion holds; otherwise layer mode-IPW. Source: `experiments/cognitive-screening/run.py:1015-1040`.
- **[P2] W6 self-reported memory (`H6DA18B`–`H6DA18F`) as a tertiary cognitive outcome.** Public-use W6 has no performance cognition but does have self-reported memory items; these are subjective but add longitudinal signal. Low priority because construct validity is weaker than the W4 battery. Ref: [dataset_manual.md §4.6](reference/dataset_manual.md).
- **[P2] Manski bounds as a fallback when IPAW is not credible.** When the attrition-probability model has large variance or sparse support in some strata, Manski-style bounds on the outcome effect (best-case / worst-case imputations for attritted cells) are more honest than a single IPAW estimate. Keep in the back pocket for handoff pairs whose IPAW fit is noisy.

---

## B. Correctness / real bugs

- **[P0] `scripts/analysis/derivation.py` — `derive_w5_bds` NaN handling.** `success = (a == 1) | (b == 1)` returns `False` when `a`/`b` contain NaN (NaN == 1 → False), so a respondent with all missing items at length L=4+ but passes at L=2,3 gets a final score of 3 — indistinguishable from a respondent who actually failed at L=4. The end-of-function mask only catches *entirely*-missing respondents. Decide: is "missing at length L" a fail (current behaviour, arguably OK per the protocol since the test terminates after two wrongs) or indeterminate (score = NaN)? If the former, add a comment clarifying; if the latter, add per-length missingness propagation. **`[JUDGMENT REQUIRED]`** — pick fail-on-NaN vs propagate-NaN; current xfail test in `tests/test_derivation.py` documents both.
- **[P0] D6 threshold drift between code and docs.** Code in the causal-screening block of [experiments/cognitive-screening/run.py](experiments/cognitive-screening/run.py) uses `abs(rho) > 0.7`; methods.md §2 says ≥ 0.6. Doc was updated 2026-04-22 to match code. Remaining: **decide whether 0.7 is the right threshold** — it's stricter than convention 0.6; consider loosening to 0.6 and documenting (would reclassify some Weakened exposures). **`[JUDGMENT REQUIRED]`**.
- **[P0] Reserve-code stripping is opt-in via `VALID_RANGES`; many outcomes/covariates silently pass through unchecked.** Ref: scientific audit. `clean_var` returns the raw numeric series untouched when `name not in VALID_RANGES`, and the screening fitters often pass *frame columns* directly into `weighted_ols`. The verification "reserve-code leakage" check only inspects columns *already in `VALID_RANGES`* — it cannot detect the case it's supposed to detect. Fix: make `clean_var` warn loudly when called on a name absent from `VALID_RANGES` (raise is too disruptive; warn + log). Source: `scripts/analysis/cleaning.py:66-71`.
- **[P0] `weighted_mean_se` does not filter NaN PSU values; pandas groupby silently drops NaN keys, undercounting H.** Ref: implementation audit. The mask filters `y` and `w` for NaN but ignores `psu`. Rows with valid `y, w` but NaN PSU pass the mask, then get dropped by `df.groupby("psu")`. They contribute to the weighted mean (numerator) but vanish from the cluster-variance sum and H count, biasing SE downward. Same root cause in `weighted_ols`. Fix: add `& ~pd.isna(psu)` to both masks. Source: `scripts/analysis/weighted_stats.py:14-35`, `scripts/analysis/wls.py:36-46`.
- **[P0] Verification block re-derives `PARENT_ED` on raw 1-9 codes instead of recoded 0-6 ordinal; race uses `.fillna(0)` then exclusion-to-Other.** Ref: implementation audit. Inside `run_verification()` lines 1394-1396 the IPW logit re-computes parent_ed using raw 1-9 max, not the canonical `derive_parent_ed` 0-6 recode. Section 7 IPW therefore models attrition on a different scale than every other regression. Race recoding silently maps missing race indicators to "Other". Fix: replace the in-block recompute with `derive_parent_ed(w1race)` and `derive_race_ethnicity(w1race)` calls. Source: `experiments/cognitive-screening/run.py:1394-1396`.
- **[P0] [JUDGMENT REQUIRED] `derive_parent_ed` recodes "don't know / other" parent-ed codes (10, 11) as post-grad (6).** Ref: implementation audit. The function's docstring says codes `10, 11 = other / DK / NA via reserves`, yet the recode does `out[x.isin([8, 10, 11])] = 6`, lumping uninformative codes with code 8 ("professional training"). `VALID_RANGES["H1RM1"] = (1, 11)` lets these through. Net effect: respondents with unknown parent-ed get systematically classified as having the most-educated parents, biasing PARENT_ED upward and inflating its adjustment power. Fix depends on canonical W1 codebook reading of codes 10/11: (a) drop them via `VALID_RANGES["H1RM1"] = (1, 9)` so `clean_var` strips to NaN; (b) split recode so `out[x == 8] = 6` and `out[x.isin([10, 11])] = np.nan`. The cleaning.py range comment ("11=never went to school") and the derivation.py docstring ("10, 11 = other / DK / NA") are mutually inconsistent — that needs resolving first. Source: `scripts/analysis/derivation.py:106-136` (line 132).
- **[P0] [JUDGMENT REQUIRED] `report.md` claim C1 ("strong") rests on a screening estimate, not a formal handoff.** Ref: scientific audit. C1 is rated **strong**, but the cited `cardiometabolic-handoff/` experiment is "planned, not started" per its own README. The only evidence is the multi-outcome screen which (i) applies the cognitive DAG uniformly, (ii) uses `GSWGT4_2` not the right weight, (iii) has not run the per-outcome D2 negative-control under the cardiometabolic DAG, (iv) lacks an E-value bound. Recommendation: demote C1 to **provisional** until cardiometabolic-handoff lands. Add a column to claims table documenting which required artefacts are still pending. User decides bar for "strong". Source: `report.md:7`.
- **[P0] [JUDGMENT REQUIRED] DAG-Cog adjustment-set justification doesn't match the drawn DAG: `PED → SOC` arrow missing.** Ref: scientific audit. The DAG bullet table claims PED is in the adjustment set as a confounder of SOC and Y, but the Mermaid diagram only shows `PED → AHPVT` and `PED → Y`, not `PED → SOC`. Either the diagram is missing an arrow (parental ed presumably affects adolescent peer integration via residential sorting, school choice) or the bullet table's confounder claim is overstated. User confirms whether `PED → SOC` belongs. Source: `reference/dag_library.md:17-91` (line 53 area).
- **[P1] `weighted_ols` does not normalise survey weights — interacts with `use_correction=True`.** Ref: scientific audit. `sm.WLS(y, X, weights=w)` passes raw survey weights (sum to ~population total). statsmodels' `use_correction=True` small-sample factor depends on σ̂² which assumes E[w·e²] = σ², only correct if weights average to 1. Fix: normalise to mean 1 (`w_norm = w * len(w) / w.sum()`) before passing to `sm.WLS`. Point estimate invariant; SE may shift slightly. Verify against `samplics` or R's `survey::svyglm` on one anchor model. Source: `scripts/analysis/wls.py:47-52`.
- **[P1] Verification IPW comparison row uses hardcoded β/p/N for the "Primary" baseline.** Ref: implementation audit. Lines 1449-1456 of cognitive-screening/run.py freeze `beta_idgx2: 0.00926, p: 0.0154, n: 3268` from a previous run. The IPW row recomputes live; the Primary row will not update if cleaning, frame construction, or spec changes. Fix: refit the same anchor (`IDGX2 → W4_COG_COMP`, `_adj_full`, `GSWGT4_2`) at the top of section 7 and pull beta/p/n from the fresh result. Two extra lines.
- **[P1] `scripts/prep/05_weighted_descriptives.py` re-implements `VALID_RANGES`, `clean_var`, `weighted_mean_se`, `weighted_prop_ci`, `derive_cesd_sum`, `derive_w5_bds` instead of importing from `analysis`.** Ref: implementation audit. Five core helpers are duplicated verbatim. Any edit to `analysis.cleaning.VALID_RANGES` or `analysis.derivation.derive_cesd_sum` will silently NOT propagate. Fix: replace local definitions with imports from `analysis.cleaning`, `analysis.weighted_stats`, `analysis.derivation`. Source: `scripts/prep/05_weighted_descriptives.py:56-245`.
- **[P1] [JUDGMENT REQUIRED] `derive_cesd_sum` in-place reverse-scoring + min_count=19 means ANY missing item zeroes the respondent.** Ref: implementation audit. The reverse-score loop modifies items 4/8/11/15 via `3 - cleaned[col]`; reverse of NaN is NaN; min_count=19 then fails for any respondent with even one missing item. Strict listwise — conservative but very strict. Either document the semantic in docstring (one-line addition) or relax to `min_count=15` (methodological judgment). Source: `scripts/analysis/derivation.py:27-32`.
- **[P1] D2 / D1 NaN vs FAIL conflation.** Ref: implementation audit. When `_d2_negctrl` or `_d1_baseline` produce a degenerate fit with p=NaN, the comparison `p > 0.10` is `False`, and the function records `pass: False` — same as a real "negative control DID predict the outcome". Classifier treats both identically. Fix: distinguish `pass: None` (could not be evaluated) from `pass: False` (evaluated and failed); skip None values from score and Drop/Weakened predicates. Source: `experiments/cognitive-screening/run.py:464-472`.
- **[P1] `_classify_screening` references `r["d2_p"] < 0.05` without NaN guard.** Ref: implementation audit. Line 679: NaN comparison silently returns False, so the branch is skipped — exposure ends up classified as something other than "Drop" purely because of missing D2 evaluation. Same for D3 lines 682-686. Fix: explicit NaN handling, e.g. `if d2 is False and pd.notna(r["d2_p"]) and r["d2_p"] < 0.05:`. Source: `experiments/cognitive-screening/run.py:679-680, 682-686`.
- **[P1] D2 (`HEIGHT_IN`) drives "Drop" classification in code, contradicting `dataset_manual.md`'s "do not drop on D2 alone" rule.** Ref: scientific audit. `_classify_screening` sets category = "Drop" for any exposure where `d2 is False and r["d2_p"] < 0.05`. This contradicts both `methods.md §2` (D2 contaminated) and `dataset_manual.md` (we do not drop on D2 alone). Any exposure with a true height-mediated peer-status pathway gets dropped despite the docs saying it shouldn't. Fix: remove the D2-Drop branch and only flag D2 fails as a comment/note. Source: `experiments/cognitive-screening/run.py:679-680`.
- **[P1] [JUDGMENT REQUIRED] `derive_friendship_grid` silently maps NaN-anchor to "not nominated".** Ref: implementation audit. `nominated = df[in_school_col].isin([0, 1])` returns False for both NaN and reserve codes. NaN at `H1MF2A` → 0 nominees for that slot. Returns `FRIEND_N_NOMINEES` as int initialised to 0 — no way to distinguish "respondent has 0 nominated friends" from "respondent has missing data on every grid item". Either document the convention (current reasonable behaviour) or track an all-missing mask analogous to `derive_w5_bds` and propagate NaN. Methods call. Source: `scripts/analysis/derivation.py:170-173`.
- **[P1] [JUDGMENT REQUIRED] `derive_friendship_grid` double-counts item 9 — included in both `FRIEND_CONTACT_SUM` and `FRIEND_DISCLOSURE_ANY`.** Ref: implementation audit. `FRIEND_ITEMS_CONTACT = [6, 7, 8, 9, 10]` includes item 9 ("talked about a problem"), which is ALSO `FRIEND_ITEM_DISCLOSURE = 9`. Contact-sum's interpretation as "non-emotional contact intensity" is wrong — it includes emotional disclosure. Either drop item 9 from `FRIEND_ITEMS_CONTACT` (4-item contact sum), or rename column / docstring to "any past-7-day interaction including emotional talk". Methods call. Source: `scripts/analysis/derivation.py:152-153, 175-183`.
- **[P1] [JUDGMENT REQUIRED] `_w5_mode_selection` Probit silently drops every NaN-IDGX2 row, restricting analysis to W1 network ∩ W1 cognitive subsample.** Ref: implementation audit. The `dropna(subset=["IS_COG_MODE", "idgx2", "male", "ahpvt"])` gates analysis to ≈4,397 of 6,504 W1, but the markdown writeup title doesn't disclose this. Whole point of audit is testing whether IDGX2 predicts mode in *full* cohort, not within a subsample that already has IDGX2 observed. Either restate title as "...within the W1-network subsample" or substitute IDGX2 with mean/zero-impute version + IDGX2-missing indicator. Researcher's target estimand. Source: `experiments/cognitive-screening/run.py:1015-1040`.
- **[P1] Dead `np` import in `scripts/analysis/cleaning.py`; dead `os` import in `scripts/prep/05_weighted_descriptives.py`.** Ref: implementation audit. `cleaning.py:11` imports numpy but only uses `pd.*`. `05_weighted_descriptives.py:41` imports os but never references it. Fix: delete the dead import lines.
- **[P1] `scripts/prep/06_attrition.py:_df_to_md` renders NaN as the string "nan", not blank.** Ref: test-coverage agent surfaced 2026-04-25. The `isinstance(v, float)` branch runs before `pd.isna(v)`, so NaN floats never reach the explicit empty-string fallback. Fix: reorder the conditional to check `pd.isna(v)` first. Test `test_df_to_md_handles_nan_renders_as_nan_string` documents the current bug.
- **[P1] `scripts/prep/05_weighted_descriptives.py:summarize_block` binary heuristic still misclassifies PRXPREST without explicit override.** Ref: test-coverage agent surfaced 2026-04-25. TODO §B P2 was resolved by adding the explicit override, not by hardening the heuristic. Pin: `test_summarize_block_prxprest_default_misclassified_binary` — any future heuristic hardening trips the test. Fix: harden heuristic to check unique-value count or dtype, not just `(lo, hi) == (0, 1)`.

---

## C. Documentation errors / discrepancies

Cross-checked across [reference/dataset_manual.md](reference/dataset_manual.md), [reference/research_journal.md](reference/research_journal.md), and [reference/variable_dictionary.md](reference/variable_dictionary.md).

- **[P0] Cognitive-screening shortlist (BCENT10X, ODGX2) contradicts the project's actual handoff plan (IDGX2 → cardiometabolic; ODGX2 → SES).** Ref: cross-doc audit. `14_screening.md:313` says "Shortlist for formal causal estimation: BCENT10X, ODGX2" but every downstream artifact (report.md C1, variable_dictionary, all 4 planned handoff experiments) names IDGX2. Fix: clarify in the auto-generated screening output that this is the *cognitive*-only shortlist and the *cross-outcome* handoff list comes from multi-outcome-screening. Edit run.py's emit-markdown step to add a one-line callout. Source: `experiments/cognitive-screening/tables/primary/14_screening.md:313`, `experiments/cognitive-screening/tables/primary/14_shortlist.csv`, `report.md:7`.
- **[P0] "Estimand wording" missing from 4 of 7 experiment field tables (cardiometabolic-handoff, ses-handoff, negative-control-battery, saturation-balance).** Ref: cross-doc audit. README.md:79 says estimand wording is mandatory. The two completed experiments and cognitive-frontdoor have it; the other four planned stubs don't. Fix: add Estimand wording row to each. Templates: cardiometabolic substitutes outcome and adjustment set into DAG-Cog estimand; SES adds "earnings bracket midpoint" and IPAW-conditioned population; NC battery's estimand is the per-NC null; saturation-balance is descriptive (note explicitly).
- **[P1] [JUDGMENT REQUIRED] Phantom experiment ID `EXP-16-IPAW-W5` referenced but never registered.** Ref: cross-doc audit. ses-handoff README and experiments/README cite this ID, but no folder, README, or row exists. Either (a) IPAW is shared infrastructure (drop the ID, rephrase as "via the planned IPAW utility (see TODO §A3)") or (b) it's a separate experiment (create stub folder + README + index row). User picks. Source: `experiments/ses-handoff/README.md:7,16`, `experiments/README.md:28`.
- **[P1] [JUDGMENT REQUIRED] `H1FS13`/`H1FS14`/`H1DA7`/`H1PR4` `kind` mismatch: code tags continuous, dictionary tags ordinal.** Ref: cross-doc audit. Code in cognitive-screening/run.py:356-359 tags these as `continuous` (drives D6 dose-response and D7 overlap branching); variable_dictionary calls them ordinal 0–3 (or 1–5 for H1PR4). Either (a) update dictionary to call them continuous (one-line edit, with a note about the linearity assumption on a 4-level scale), or (b) add an `ordinal` kind that routes to the continuous-D6 branch but documents the ordinal nature. Author preference on taxonomy. Source: `experiments/cognitive-screening/run.py:356-359`, `reference/variable_dictionary.md:209-217`.
- **[P1] D5 pass criterion in `methods.md` omits the `n_sig ≥ 2` requirement.** Ref: cross-doc audit. methods.md §2 D5 row says "All three component β in the same direction as the composite, no opposite-sign dissent" but code requires sign consistency AND ≥ 2 of 3 components significant at p<0.10. Fix: update methods.md §2 D5 row to add "**AND ≥ 2 of 3 with p < 0.10**". Source: `reference/methods.md:63`, `experiments/cognitive-screening/run.py:535-536`.
- **[P1] W5 cognitive cell N described inconsistently across docs (824 vs 624 vs 620).** Ref: cross-doc audit. methods.md attributes 824 to network intersection (factually wrong — it's W1 in-home); glossary says 624 (unsourced); dataset_manual says 620–625 (per-item administered). Network ∩ W5 cognitive is closer to 394. Fix: unify wording. Canonical: "824 = W1 in-home ∩ W5 cognitive (mode I/T); ~620 = administered per item across the full W5 sample; ~394 = W1 network ∩ W5 cognitive (network-gated)." Source: `reference/methods.md:38`, `reference/glossary.md:61`, `reference/dataset_manual.md:178`, `reference/variable_dictionary.md:40,140`, `scripts/prep/outputs/08_analytic_frame_n.md:68`.
- **[P1] [JUDGMENT REQUIRED] `report.md` cites IDGX2 → H4BMICLS as Strong but the screen treats it as linear-on-ordinal.** Ref: cross-doc audit. variable dictionary and methods.md call out linear-on-ordinal as wrong for formal estimation; the report should not cite the screen β as strong evidence on the very outcome where the linear-in-Y assumption is the load-bearing weakness. Fix: add parenthetical "(screen-only β; ordered-logit re-estimation pending in cardiometabolic-handoff)" or downgrade to "provisional → strong on H4WAIST/H4BMI, provisional on H4BMICLS". Author judgment on strength rating. Source: `report.md:24`.
- **[P1] `legacy/dataset_eda.ipynb` is described as moved but lives at repo root.** Ref: cross-doc audit + verification 2026-04-26. `dataset_eda.ipynb` (105 KB) is still at the project root despite Phase 4 Agent G's report claiming it was moved to `legacy/`. The `legacy/` directory does not exist. Fix: `mkdir legacy && git mv dataset_eda.ipynb legacy/` — the move that was supposed to happen.
- **[P1] `ATE` used throughout methods.md / dataset_manual.md but undefined in glossary.** Ref: cross-doc audit. ATE (Average Treatment Effect) is the primary estimand label, appearing 5× in methods.md and elsewhere, but no glossary entry. Fix: add ATE entry alphabetically (just before "Back-door path") in `reference/glossary.md` defining the term, with a "why it matters" line linking to methods.md §1. Source: `reference/methods.md:17,36,38,43,101`, `reference/dataset_manual.md:301`, `reference/glossary.md` (no entry).
- **[P1] Stale "task14"/"task10"/"task15" jargon in auto-generated experiment outputs and variable_dictionary.** Ref: cross-doc audit. `14_screening.md:3` references "the same adjustment set conventions used in task10"; `15_multi_outcome.md:180` says "Cross-reference with task14's D2/D6/D7"; `variable_dictionary.md:80,225,47` all reference "task 14 D2", "task 10–14 analysis", "task 10–15 fits". The new contributor won't find a `task10` anywhere. Fix: edit auto-generation strings in cognitive-screening/run.py and multi-outcome-screening/run.py to use new experiment-folder names, then re-run; sweep variable_dictionary.md for "task 10/14/15" → experiment-folder names.
- **[P1] Broken anchor: research_journal cites `variable_dictionary.md#5-secondary-outcomes-task-15-12-total`.** Ref: cross-doc audit. The variable dictionary's section is `## 2.5 Secondary outcomes — task 15 multi-outcome extension (12)`, generating anchor `#25-secondary-outcomes--task-15-multi-outcome-extension-12`. Journal's link does not exist as a heading. Fix: update journal link to `[variable_dictionary.md §2.5](variable_dictionary.md#25-secondary-outcomes--task-15-multi-outcome-extension-12)`. Source: `reference/research_journal.md:146`.
- **[P1] `scripts/prep/04_missingness.py:infer_scheme` more conservative than its docstring.** Ref: test-coverage agent surfaced 2026-04-25. A 1-digit Likert with 6/7/8/9 reserves is only flagged `1digit` if ≥80% of values fall in [0, 5]. A 50/50 split returns `none`. Worth adding a comment in the source pointing this out so callers don't expect the simple "max ≤ 9" criterion.
- **[P2] Variable dictionary lists families but not their full members.** CES-D items `H1FS1`–`H1FS19`, friendship grid `H1MF*`/`H1FF*` items, BEM `H3BM*`, W5 BDS `H5MH*` — only a few exemplar codes are listed. Decide whether to (a) link to the existing per-family files like [scripts/prep/outputs/03a_wave1_friendship_items.md](scripts/prep/outputs/03a_wave1_friendship_items.md), or (b) accept that the dictionary doesn't enumerate family members (current state, lightly documented). Option (a) is a one-sentence pointer per family.
- **[P2] dataset_manual.md §6 "Analytic feasibility" table** still uses "W5 imm" and "W5 backward digit span (derived)" language that conflicts with the §9-era framing of W5 as part of the multi-outcome battery. Mild; the §6 table is about design feasibility, but a one-line note tying §6 row "W1 network ∩ W5 imm" to the expanded outcome battery (now documented in [variable_dictionary.md §2.5](reference/variable_dictionary.md#25-secondary-outcomes--task-15-multi-outcome-extension-12)) would help a new reader see how multi-outcome screening relates to cognitive screening.
- **[P2] N drift between synthesis and journal.** Synthesis §3.1 quotes the raw network-sample N = 4,397; journal Phase 2 quotes the primary analytic frame N = 3,238; dictionary quotes N = 3,268 for IDGX2 in the task14 frame. All three are correct for different stages of the pipeline (raw → mode-OK cognition-merged → covariate-joined), but a one-line note explaining the "4,397 → 3,268 → 3,238" cascade would remove confusion.
- **[P2] [JUDGMENT REQUIRED] `[TODO.md A7]` style links are informal — TODO.md is unnumbered.** Ref: cross-doc audit. experiments/README.md:22,28 and others reference "TODO.md A7" but TODO.md uses lettered sections without numbered bullets, so "A7" is fragile to bullet additions. Either (a) introduce explicit bullet numbering in TODO.md §A so `A7` resolves, or (b) replace with "(per the E-value sensitivity item in [TODO.md §A](../TODO.md#a-research-pipeline--task-16-and-beyond))". Depends on stability the user wants for bullet IDs.
- **[P2] Variable dictionary quick-ref omits `C5WD60_1`.** Ref: cross-doc audit. `C5WD60_1` (delayed-recall counterpart) is referenced in W5 composite derivation but missing from alphabetical quick-ref. Fix: add one row to quick-ref table with W5 / "S19 # WORDS ON LIST RECALLED 60 SEC-W5" / W5 analogue of C4WD60_1.
- **[P2] dataset_manual.md Changelog references stale path `scripts/task05_weighted_descriptives.py`.** Ref: cross-doc audit. Line 330 mentions the old path inline (not as a markdown link, so doesn't 404). Fix: optionally clean up the parenthetical "(since moved to [scripts/prep/05_weighted_descriptives.py](...))" by deleting the leading old-path reference. Source: `reference/dataset_manual.md:330`.

---

## D. Reproducibility / infrastructure

- **[P1] No top-level orchestrator.** No `run_all.py`, `Makefile`, `invoke` / `just` / `snakemake` entry point. A new contributor must read [experiments/README.md](experiments/README.md) to guess the run order. Write a minimal `run_all.py` that invokes the prep pipeline ([scripts/prep/01_file_inventory.py](scripts/prep/01_file_inventory.py) → … → [scripts/prep/09_distribution_plots.py](scripts/prep/09_distribution_plots.py)) followed by [experiments/cognitive-screening/run.py](experiments/cognitive-screening/run.py) and [experiments/multi-outcome-screening/run.py](experiments/multi-outcome-screening/run.py) with `--skip-if-fresh` semantics.
- **[P2] Print-based logging throughout.** Scripts use `print()` for progress. No way to suppress or route. Low priority until the repo gets a CI or long-running orchestrator; swap to `logging` module with a configurable level when you do.
- **[P2] SCC deployment path.** User's global instructions note Boston University SCC access with project work under `/projectnb/mcnet/jbrin/`. The pipeline is small enough to run on a laptop, but a sanity check that the scripts tolerate the `$TMPDIR` / `module load miniconda` environment would de-risk moving heavier analyses (Task 16 IPAW re-runs, bootstrap CIs) to the cluster.
- **[P2] Stub modules `analysis.diagnostics`, `analysis.frontdoor`, `analysis.ipw`, `analysis.sensitivity` advertised in __init__.py but only contain docstrings.** Ref: implementation audit. `from analysis.ipw import fit_ipaw` will fail with `ImportError`, not a helpful "not yet implemented" message. Fix: each stub file's body becomes `raise NotImplementedError("...see TODO §A...")` at import-time, OR rewrite `__init__.py` summary to flag them as TODO with the open-issue ID.
- **[P2] EXPOSURES registry duplicated between cognitive-screening and multi-outcome-screening.** Ref: implementation audit. `multi-outcome-screening/run.py:65-106` inlines the entire registry from cognitive-screening for self-containment, with an inline comment acknowledging the maintenance hazard. Fix: move `NETWORK_EXPOSURES`, `SURVEY_EXPOSURES`, `EXPOSURES`, `RED_FLAGS`, `SIBLINGS`, `RACE_LEVELS`, `_adj_*`, `ADJ_BUILDERS` into `scripts/analysis/diagnostics.py` (currently a stub). Both experiments import from there. Killss the duplicated-stub problem above too.
- **[P2] `analysis.plot_style.IMG = ROOT / "img"` and `analysis.plot_style.save` are dead code.** Ref: implementation audit. Repo-wide grep confirms no caller imports `plot_style.IMG` or calls `plot_style.save`. The `img/` directory does not exist (deleted by Phase 4 Agent G). Three figure modules each define their own `_save` helper. Fix: delete `IMG`, `save()`, and the stale comment in 09_distribution_plots.py:36; OR repurpose `save` as the canonical write helper. Source: `scripts/analysis/plot_style.py:12, 67-73`.
- **[P2] `_save` helper duplicated in three figure modules.** Ref: implementation audit. `experiments/cognitive-screening/figures.py:64-69`, `experiments/multi-outcome-screening/figures.py:55-59`, `scripts/prep/09_distribution_plots.py:40-46` — three near-identical helpers. Refactor `analysis.plot_style.save` to take an absolute destination Path, then have all three callers import it. Couples with the dead-code finding above.

---

## E. Plot & output hygiene

- **[P1] D9 column header in screening heatmap reads "D9 red flag" but green = no red flag (good); inverted vs. surrounding columns.** Ref: implementation audit. The colour-mapping branch at `figures.py:597-604` correctly inverts D9 (red flag → red cell), but the column header doesn't telegraph this. Visual labelling fragile. Fix: rename column header to `"D9 (no red flag)"` so green-means-good lines up with the colour. Source: `experiments/cognitive-screening/figures.py:597-604`.
- **[P2] `derive_w5_bds` `dtype=float` cast at line 37 is load-bearing but undocumented.** Ref: implementation audit. Initialising `score = np.zeros(n, dtype=float)` is required so `out[all_missing_mask] = np.nan` works. A maintainer who "tightens" to int will silently break the all-missing-NaN case. Add a one-line comment. Source: `scripts/analysis/derivation.py:35-50`.

---

## F. Nice-to-haves / stretch

- **[P2] Plain-text DAG diagram** in [methods.md §1](reference/methods.md#1-identification-assumptions-and-target-estimand). Described textually; rendering as a small `mermaid` block or PNG would be useful for briefings (note: [dag_library.md](reference/dag_library.md) already has the canonical Mermaid `DAG-Cog` block).
- **[P2] Pre-commit hook or CI check** that re-runs the reference-doc cross-consistency audit (quick-ref vs. detailed vs. journal vs. dictionary) and fails on broken anchors / missing variable entries. The audit script in `/tmp` during this session could be crystallised as `scripts/audit_references.py`.
- **[P2] Dictionary quick-ref could support search by role.** Right now it's alphabetical; a reader looking at a forest plot sees `IDGX2 → H5EC1` and wants *all exposures in that forest plot's cluster*. A secondary by-role index at the bottom of the quick-ref would help (or just a link back to the detailed §2.3 subsections).

---

## G. Session-completed items (for traceability)

- ~~PRXPREST binary-vs-continuous miscoding fixed in [scripts/prep/05_weighted_descriptives.py](scripts/prep/05_weighted_descriptives.py) + [scripts/prep/outputs/05_weighted_descriptives.md](scripts/prep/outputs/05_weighted_descriptives.md) re-run.~~ **Done 2026-04-22.**
- ~~D3 sibling-dissociation sign-agreement bug.~~ **Fixed earlier in this conversation thread; verified in the causal-screening block of [experiments/cognitive-screening/run.py](experiments/cognitive-screening/run.py).**
- ~~Baked-in titles stripped from the 7 PNGs embedded in `research_journal.md`.~~ **Done 2026-04-22.**
- ~~Variable dictionary restructured into quick-ref + detailed sections.~~ **Done 2026-04-22.**
- ~~Synthesis §5.6 identification-assumptions section, §6.5 D1–D9 methods, Glossary appendix added.~~ **Done 2026-04-22.** (Subsequently moved into [reference/methods.md](reference/methods.md) §1–§2 and [reference/glossary.md](reference/glossary.md) by the Phase 3 demolition.)
- ~~Stata/R recipes removed from synthesis §2.4 (project is Python-only).~~ **Done 2026-04-22.**
- ~~Broken anchor `#45-wave-v-cognitive-battery-...` fixed.~~ **Done 2026-04-22.**
- ~~Stale "earlier notes were wrong" self-references audited and removed or trimmed once source fixes landed.~~ **Done 2026-04-22.**
- ~~`requirements.txt` was incomplete (missing `seaborn`, `statsmodels`, `pyreadstat`, `pyarrow`, `scipy`).~~ **Done 2026-04-25.**
- ~~Hardcoded absolute path `/Users/jb/Desktop/Inter-Generational-Causal-Analysis-of-the-AddHealth-Dataset` in 10+ files.~~ **Done 2026-04-25 — replaced with `ROOT = Path(__file__).resolve().parent.parent`.**
- ~~README was effectively empty.~~ **Done 2026-04-26 — Phase 4 Agent F wrote a real README with project goal, data acquisition, install, run, layout, and "adding an experiment" sections.**
- ~~No test suite.~~ **Done 2026-04-26 — Phase 1 created `tests/`; test-coverage agent expanded to 133 tests covering 90% of `scripts/analysis/`.**
- ~~Repo restructure (5 phases): scripts/analysis library, experiment-folder layout, methods.md / glossary.md split, README + experiments/README, cleanup.~~ **Done 2026-04-25 / 2026-04-26.**
- ~~Add `tabulate` to requirements.txt.~~ **Done 2026-04-25 — added by Phase 4 Agent C.**
- ~~Cache directory move from `outputs/cache/` to top-level `cache/`.~~ **Done 2026-04-26 — Phase 4 Agent G + Phase 5 prep-script CACHE constant patches.**
- ~~Misleading comment in `derive_school_belonging`.~~ **Done 2026-04-25.**
- ~~Hardcoded saturation fraction `0.324` replaced with `SATURATED_LOSS_FRAC` computed at import-time.~~ **Done 2026-04-25.**
- ~~Variable dictionary missing `C5WD90_1`, `H1FS1`, `W5_COG_COMP` from quick-ref.~~ **Done 2026-04-25.**
- ~~Missing dictionary entries for `C4WD60`, `C4WD90`, `H1GH28` referenced by journal.~~ **Done 2026-04-25.**
- ~~Synthesis deliverables index missing task09, task12, task15_journal_figs.~~ **Done 2026-04-25 (and the deliverables index itself was subsequently subsumed by `experiments/README.md` + `report.md` in the Phase 3 demolition).**
- ~~`outputs/cache/` tracked-by-mistake check.~~ **Verified 2026-04-25 — properly ignored.**
- ~~Re-run prep distribution plots + cognitive-screening figures to confirm no baked-in titles remain.~~ **Done 2026-04-25.**
- ~~`H5LM5` "despite earlier notes" caveat cleanup.~~ **Done 2026-04-25.**
- ~~Consistent colourmap for outcome groups across charts.~~ **Done 2026-04-25 — `GROUP_COLORS` canonicalized in `scripts/analysis/plot_style.py`.**
- ~~Repo's canonical citation in `CITATION.cff`.~~ **Done 2026-04-25 — ICPSR 21600 v26 dated 2026-03-03, DOI flagged `[verify]`.**

---

## Audit pass (2026-04-26) summary

A three-agent audit on 2026-04-26 (scientific methodology, code correctness, cross-doc consistency) plus a test-coverage expansion surfaced 53 new findings folded into the sections above. Severity tally for the new items:

- **B (correctness):** 8 P0, 13 P1, 0 P2 — 8 are `[JUDGMENT REQUIRED]`
- **C (docs):** 2 P0, 7 P1, 4 P2 — 4 are `[JUDGMENT REQUIRED]`
- **A (research pipeline):** added 7 new analytic items — 5 are `[JUDGMENT REQUIRED]`
- **D (infrastructure):** 4 P2
- **E (plot hygiene):** 1 P1, 1 P2

Auto-fix-eligible (not `[JUDGMENT REQUIRED]`, severity P0 or P1, mechanical fix) is dispatched by the fix phase that follows this consolidation. `[JUDGMENT REQUIRED]` items are deferred to user review and listed verbatim in the post-fix report.
